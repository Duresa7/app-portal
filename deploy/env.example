# Copy to deploy/.env. Compose reads this file for variable interpolation only.
# Pin the listener to one interface and port; omit the address to listen on all interfaces.
APP_PORTAL_BIND=192.168.40.35:3004
