# Docs

- `images/apps.png`: the Apps view with one install queued, rendered from the fake Action1 backend.
- `images/activity.png`: the Activity view after that install completed.

Both images were produced with `dotnet run -- --screenshot <file> <section>` under Xvfb; they contain no data from a real tenant.
